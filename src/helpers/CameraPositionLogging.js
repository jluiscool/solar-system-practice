import { useThree } from "@react-three/fiber";
import { useEffect, useRef } from "react";

function CameraPositionLogging() {

    const { camera } = useThree;

    return (
        null
    )
}

export default CameraPositionLogging;